tstc lhbr psiz hekg
zmkk faqi mxav rgsx


echo 'export DOTNET_DEV_EMAIL_PASS="zmkk faqi mxav rgsx"' >> ~/.zshenv
source ~/.zshenv
# This script sets an environment variable for the email password`  
# and appends it to the .bashrc file so that it persists across terminal sessions.
# Make sure to replace "your-email-password" with the actual password.